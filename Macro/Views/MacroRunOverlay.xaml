<Window x:Class="Macro.Views.MacroRunOverlay"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Macro status"
        Width="278" Height="72"
        WindowStyle="None" ResizeMode="NoResize"
        AllowsTransparency="True" Background="Transparent"
        ShowInTaskbar="False" ShowActivated="False"
        Focusable="False" IsHitTestVisible="False">
    <Border Background="#E61B2024" BorderBrush="#884BCB91" BorderThickness="1"
            CornerRadius="12" Padding="14,10">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="12" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Ellipse Width="8" Height="8" Fill="#4BCB91" VerticalAlignment="Center" />
            <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="8,0,0,0">
                <TextBlock Text="Macro is Running" Foreground="White" FontSize="15" FontWeight="SemiBold" />
                <TextBlock x:Name="ShortcutText" Text="Ctrl + Shift + F10 to stop" Foreground="#C8D0D5" FontSize="11" Margin="0,3,0,0" />
            </StackPanel>
        </Grid>
    </Border>
</Window>
